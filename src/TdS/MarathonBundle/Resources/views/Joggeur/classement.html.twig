{% extends "TdSMarathonBundle::layout.html.twig" %}

{% block title %}
  Marathon - {{ parent() }}
{% endblock %}

{% block tdsmarathon_body %}
	<div id="content" class="bg--lightgrey pam pbxxl">
		<div class="classement">
			<h1 class="main-title man pbm align-center">
				{# <svg class="icon icon--s icon-plus"><use xlink:href="#icon-plus"></use></svg> #}
				<span>Classement</span>
			</h1>
			{# <div class="row row-header sep--dashed pas">
					<h3 class='col col-md-5 man pan'>Joggeur</h3>
					<div>
						<div class='col col-md-3'>
							<strong class="glyphicon glyphicon-tower">												
								Score Total						
							</strong>
						</div>
						<div class='col col-md-2'>
							<span class="glyphicon glyphicon-heart">
								derniers points coeurs
							</span>
						</div>
						<div class='col col-md-2'>
							<span class="glyphicon glyphicon-scale">
								derniers points vitesse
							</span>
						</div>
					</div>
			</div> #}
			{# {% for joggeur in listeJoggeurs %}
				<div class="row sep--dashed pas">
					<h3 class='col col-md-5 man pan'>{{ loop.index }} - {{ joggeur.pseudo }}</h3>
					<div>
						<div class='col col-md-3'>
							<strong class="glyphicon glyphicon-tower">												
								{{ joggeur.score }}						
							</strong>
						</div>
						<div class='col col-md-2'>
							<span class="glyphicon glyphicon-heart">
								{{ joggeur.lastheartpoints }}
							</span>
						</div>
						<div class='col col-md-2'>
							<span class="glyphicon glyphicon-scale">
								{{ joggeur.lastfastpoints }}
							</span>
						</div>
					</div>
				</div>		
			{% endfor %} #}
			{# <div class="sep--dashed">
				{% for joggeur in listeJoggeurs %}
					{% for score in joggeur.scores %}
						 <h2 class="sub-title pbn mbn">{{ score.id }} - {{ score.joggeur.pseudo }}</h2>
						 {{ score.theme.titre }} - {{ score.theme.saison.titre }} //

						<span> points coeurs : {{ score.heartpoints }} </span>
						<span>points vitesse : {{ score.fastpoints }}</span> 
					{% endfor %}
					{% for musicTitle in joggeur.musicTitles %}
					{% endfor %}	
				{% endfor %}
			</div> #}

			<div class="sep">
				
      			{% for joggeurScore in listeJoggeurScore %}
      				<div class="sep">
	      				<h2 class="sub-title man">{{ loop.index }} - {{ joggeurScore.joggeur.pseudo }} / {{ joggeurScore.total }}</h2>
	      				{% for score in joggeurScore.scores %}
	      					<div class="pbs sep--dashed">
	      						<span class="pls mls block">{{ score.theme.titre }} : {{ score.totalTheme }}
	      						(points coeurs : {{ score.heartpoints }}, points vitesse : {{ score.fastpoints }})</span>
	      					</div>
		      			{% endfor %}
	      			</div> 
      			{% endfor %}
			</div>

			</div>
		</div>
	</div>
{% endblock %}