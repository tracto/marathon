{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show center form-group tc">
    <div class="f4"><strong>{{ 'profile.show.username'|trans }}:</strong> {{ user.username }}</div>
    <div><strong>{{ 'profile.show.email'|trans }}:</strong> {{ user.email }}</div>
</div>